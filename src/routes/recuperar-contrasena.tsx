import { Link } from "react-router-dom";
import { useState } from "react";
export function RecoverPage() { const [sent,setSent]=useState(false); return <div className="container-x py-10"><div className="mx-auto max-w-md rounded-lg border border-border bg-card p-6"><h1 className="text-2xl font-black">Recuperar contraseña</h1>{sent?<p className="mt-4 rounded bg-green-50 p-3 text-sm text-green-800">Enviamos las instrucciones a tu correo.</p>:<form onSubmit={(e)=>{e.preventDefault();setSent(true)}} className="mt-5 space-y-3"><input required type="email" placeholder="tu@correo.com" className="w-full rounded border border-border bg-background px-3 py-2 text-sm"/><button className="btn-primary w-full">Enviar enlace</button></form>}<Link to="/login" className="mt-4 inline-block text-sm text-primary hover:underline">Volver a iniciar sesión</Link></div></div>; }
